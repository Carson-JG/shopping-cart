import { Layout } from "antd";

const { Footer } = Layout;

export default () => {
  return (
    <Footer style={{ textAlign: "center" }}>
      Created by Nick Carson for Jahnel Group
    </Footer>
  );
};
